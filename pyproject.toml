[project]
name = "i2c-ros2"
version = "0.1.0"
description = ""
authors = [
    {name = "Nymphaea",email = "mail@supaku.cn"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
